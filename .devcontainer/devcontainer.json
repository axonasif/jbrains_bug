{
	"build": {
		"dockerfile": "Dockerfile"
	},
	"onCreateCommand": {
		"patch-sshd": "sudo sed -i '/^#!\\/bin\\/sh$/a unset SSH_CONNECTION SSH_CLIENT SSH_TTY\\nexport DISPLAY=:0' /usr/local/gitpod/openssh/sshdwrapper.sh",
		"setup-xdg-open": "printf '%s\\n' '#!/bin/bash' '$BROWSER \"$1\"' | sudo tee /usr/bin/xdg-open > /dev/null && sudo chmod +x /usr/bin/xdg-open"
	},
	"customizations": {
		"jetbrains": {
			"plugins": [
				"com.github.copilot"
			]
		}
	}
}
